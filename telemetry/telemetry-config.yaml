# ContainerClaw Telemetry — Flink Job Configuration
# Fluss-native: no external databases, no JDBC.

source:
  fluss:
    bootstrap_servers: "coordinator-server:9123"
    database: "containerclaw"

job:
  state_ttl_hours: 4
